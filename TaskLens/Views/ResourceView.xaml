<UserControl x:Class="TaskLens.Views.ResourceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
             
    <UserControl.Resources>
        <!-- 🎨 Modern DataGrid Row Style -->
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="{DynamicResource CardBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Height" Value="50"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentSecondaryBrush}"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- 📊 Chart Container Style -->
        <Style x:Key="ChartContainerStyle" TargetType="Border" BasedOn="{StaticResource ModernCardStyle}">
            <Setter Property="Padding" Value="24"/>
            <Setter Property="Margin" Value="10"/>
        </Style>
        
        <!-- 🖱️ Modern Context Menu -->
        <ContextMenu x:Key="ProcessContextMenu" 
                     Background="{DynamicResource CardBrush}"
                     BorderBrush="{DynamicResource BorderBrush}"
                     BorderThickness="1">
            <ContextMenu.Resources>
                <Style TargetType="MenuItem">
                    <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                    <Setter Property="Padding" Value="12,8"/>
                    <Setter Property="FontSize" Value="14"/>
                    <Style.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ContextMenu.Resources>
            <MenuItem Header="🔴 작업 끝내기" Click="KillProcess_Click" />
            <MenuItem Header="📁 파일 위치 열기" Click="OpenFileLocation_Click" />
        </ContextMenu>
    </UserControl.Resources>
    
    <Grid Margin="10" Background="{DynamicResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="300"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 🌟 Hero Section -->
        <StackPanel Grid.Row="0" Margin="0,0,0,30">
            <TextBlock Text="Resource Analytics" Margin="20 20 20 0"
                       FontSize="32" 
                       FontWeight="Light" 
                       Foreground="{DynamicResource TextBrush}"/>
            <TextBlock Text="실시간 시스템 리소스 사용량 분석 및 모니터링" 
                       FontSize="16" 
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       Margin="20 20 20 0"/>
        </StackPanel>

        <!-- 📊 Charts Section -->
        <Grid Grid.Row="1" Margin="10 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 💻 CPU Chart -->
            <Border Grid.Column="0" Style="{StaticResource ChartContainerStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                        <Border Background="{DynamicResource CpuCardBrush}" 
                                CornerRadius="8" 
                                Width="32" 
                                Height="32" 
                                Margin="0,0,12,0">
                            <TextBlock Text="💻" 
                                      FontSize="16" 
                                      HorizontalAlignment="Center" 
                                      VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="CPU Usage" 
                                      FontSize="18" 
                                      FontWeight="SemiBold" 
                                      Foreground="{DynamicResource TextBrush}"/>
                            <TextBlock Text="Processor Performance" 
                                      FontSize="12" 
                                      Foreground="{DynamicResource TextSecondaryBrush}"/>
                        </StackPanel>
                    </StackPanel>
                    
                    <lvc:CartesianChart Grid.Row="1" 
                                       Series="{Binding CpuSeries}"
                                       Background="Transparent"
                                       Foreground="{DynamicResource TextBrush}"/>
                </Grid>
            </Border>

            <!-- 🧠 RAM Chart -->
            <Border Grid.Column="2" Style="{StaticResource ChartContainerStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                        <Border Background="{DynamicResource RamCardBrush}" 
                                CornerRadius="8" 
                                Width="32" 
                                Height="32" 
                                Margin="0,0,12,0">
                            <TextBlock Text="🧠" 
                                      FontSize="16" 
                                      HorizontalAlignment="Center" 
                                      VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Memory Usage" 
                                      FontSize="18" 
                                      FontWeight="SemiBold" 
                                      Foreground="{DynamicResource TextBrush}"/>
                            <TextBlock Text="RAM Utilization" 
                                      FontSize="12" 
                                      Foreground="{DynamicResource TextSecondaryBrush}"/>
                        </StackPanel>
                    </StackPanel>
                    
                    <lvc:CartesianChart Grid.Row="1" 
                                       Series="{Binding RamSeries}"
                                       Background="Transparent"
                                       Foreground="{DynamicResource TextBrush}"/>
                </Grid>
            </Border>
        </Grid>

        <!-- 🔥 Process Dashboard -->
        <Border Grid.Row="3" Style="{StaticResource ModernCardStyle}" Padding="24" Margin="20 0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- Section Header -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
                    <Border Background="{DynamicResource AccentBrush}" 
                            CornerRadius="8" 
                            Width="32" 
                            Height="32" 
                            Margin="0,0,12,0">
                        <TextBlock Text="⚡" 
                                  FontSize="16" 
                                  HorizontalAlignment="Center" 
                                  VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Top Processes" 
                                  FontSize="20" 
                                  FontWeight="SemiBold" 
                                  Foreground="{DynamicResource TextBrush}"/>
                        <TextBlock Text="리소스 사용량 기준 상위 프로세스 (F2: AI 분석)" 
                                  FontSize="12" 
                                  Foreground="{DynamicResource TextSecondaryBrush}"/>
                    </StackPanel>
                </StackPanel>

                <!-- Modern DataGrid -->
                <DataGrid Grid.Row="1"
                         x:Name="ProcessGrid" 
                         ItemsSource="{Binding TopProcessList}"
                         SelectedItem="{Binding SelectedProcess}" 
                         AutoGenerateColumns="False" 
                         IsReadOnly="True" 
                         HeadersVisibility="Column" 
                         PreviewKeyDown="ProcessGrid_PreviewKeyDown"
                         PreviewMouseRightButtonDown="ProcessGrid_PreviewMouseRightButtonDown"
                         GridLinesVisibility="None" 
                         Background="Transparent"
                         BorderBrush="Transparent"
                         BorderThickness="0"
                         SelectionUnit="FullRow"
                         ContextMenu="{StaticResource ProcessContextMenu}">
                    
                    <DataGrid.Resources>
                        <!-- Modern Header Style -->
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="Padding" Value="16,12"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        </Style>
                        
                        <!-- Modern Cell Style -->
                        <Style TargetType="DataGridCell">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="16,8"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGrid.Resources>
                    
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="Process" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Border Background="{DynamicResource SurfaceBrush}" 
                                               CornerRadius="6" 
                                               Width="32" 
                                               Height="32" 
                                               Margin="0,0,12,0">
                                            <Image Source="{Binding Icon}" 
                                                  Width="20" 
                                                  Height="20"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                                        </Border>
                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Name}" 
                                                      FontWeight="SemiBold"
                                                      FontSize="14"
                                                      Foreground="{DynamicResource TextBrush}"/>
                                            <TextBlock Text="Active Process" 
                                                      FontSize="11"
                                                      Foreground="{DynamicResource TextSecondaryBrush}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        
                        <DataGridTemplateColumn Header="Memory Usage" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Ram, StringFormat={}{0:F1} MB}" 
                                                  FontWeight="SemiBold"
                                                  FontSize="14"
                                                  Foreground="{DynamicResource TextBrush}"/>
                                        <Border Background="{DynamicResource SurfaceBrush}" 
                                               CornerRadius="2" 
                                               Height="4" 
                                               Margin="0,4,0,0">
                                            <Border Background="{DynamicResource RamCardBrush}" 
                                                   CornerRadius="2" 
                                                   HorizontalAlignment="Left"
                                                   Width="50"/>
                                        </Border>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- 🤖 AI Analysis Result -->
        <Border Grid.Row="4" 
                Style="{StaticResource ModernCardStyle}" 
                Padding="24" 
                Margin="20"
                MinHeight="120">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Border Grid.Column="0"
                        Background="{DynamicResource AccentBrush}" 
                        CornerRadius="12" 
                        Width="48" 
                        Height="48" 
                        Margin="0,0,20,0"
                        VerticalAlignment="Top">
                    <TextBlock Text="🤖" 
                              FontSize="24" 
                              HorizontalAlignment="Center" 
                              VerticalAlignment="Center"/>
                </Border>
                
                <StackPanel Grid.Column="1">
                    <TextBlock Text="AI Process Analysis" 
                              FontSize="18" 
                              FontWeight="SemiBold" 
                              Foreground="{DynamicResource TextBrush}"
                              Margin="0,0,0,8"/>
                    <TextBlock Text="{Binding AiResultText, FallbackValue='프로세스를 선택하고 F2 키를 눌러 AI 분석을 시작하세요.'}" 
                              FontSize="14" 
                              TextWrapping="Wrap"
                              LineHeight="20"
                              Foreground="{DynamicResource TextSecondaryBrush}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
