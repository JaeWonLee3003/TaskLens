<UserControl x:Class="TaskLens.Views.WarningLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="15" Background="#2D2D30">
        <StackPanel Margin="5">
            <TextBlock Text="경고 로그"
                       FontSize="24" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>
            <ListBox ItemsSource="{Binding Warnings}" Background="#2D2D30" BorderBrush="Transparent">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#3A3A3A" CornerRadius="8" Padding="10" Margin="0,5,0,5" BorderBrush="#555" BorderThickness="1">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="⚠️" FontSize="20" Margin="0,0,10,0"/>
                                <TextBlock Text="{Binding}" FontSize="16" Foreground="White" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</UserControl>
