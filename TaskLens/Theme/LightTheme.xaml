<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Base Colors -->
    <Color x:Key="PrimaryColor">#FFFFFFFF</Color>
    <Color x:Key="SecondaryColor">#FFF8F9FA</Color>
    <Color x:Key="SurfaceColor">#FFF1F3F4</Color>
    <Color x:Key="AccentColor">#FF1A73E8</Color>
    <Color x:Key="AccentSecondaryColor">#FF4285F4</Color>

    <!-- Brushes -->
    <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource PrimaryColor}" />
    <SolidColorBrush x:Key="CardBrush" Color="{StaticResource SecondaryColor}" />
    <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}" />
    <SolidColorBrush x:Key="TextBrush" Color="#FF202124" />
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="#FF5F6368" />
    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}" />
    <SolidColorBrush x:Key="AccentSecondaryBrush" Color="{StaticResource AccentSecondaryColor}" />
    <SolidColorBrush x:Key="BorderBrush" Color="#FFE8EAED" />

    <!-- Card Colors -->
    <Color x:Key="CardBackgroundColor">#FFF8F9FA</Color>
    <Color x:Key="CardBackgroundColorSecondary">#FFFFFFFF</Color>
    <SolidColorBrush x:Key="CardBorderBrush" Color="#FFE8EAED" />

    <!-- Status Colors -->
    <SolidColorBrush x:Key="InfoBrush" Color="#FF1A73E8" />
    <SolidColorBrush x:Key="SuccessBrush" Color="#FF34A853" />
    <SolidColorBrush x:Key="WarningBrush" Color="#FFEA4335" />

    <!-- Modern Card Colors with Gradients -->
    <LinearGradientBrush x:Key="CpuCardBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#FF4285F4" Offset="0" />
        <GradientStop Color="#FF1A73E8" Offset="1" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="RamCardBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#FF34A853" Offset="0" />
        <GradientStop Color="#FF188038" Offset="1" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="WarningCardBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#FFEA4335" Offset="0" />
        <GradientStop Color="#FFD33B2C" Offset="1" />
    </LinearGradientBrush>

    <!-- Glassmorphism Effect -->
    <SolidColorBrush x:Key="GlassBrush" Color="#22FFFFFF" />
    <SolidColorBrush x:Key="GlassBorderBrush" Color="#11FFFFFF" />

    <!-- Shadow Effects -->
    <DropShadowEffect x:Key="CardShadow"
                      Color="#1A000000"
                      Direction="270"
                      ShadowDepth="4"
                      BlurRadius="12"
                      Opacity="0.1" />

    <DropShadowEffect x:Key="ElevatedShadow"
                      Color="#26000000"
                      Direction="270"
                      ShadowDepth="8"
                      BlurRadius="20"
                      Opacity="0.15" />

    <!-- Modern Button Style -->
    <Style x:Key="ModernButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource AccentBrush}" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="16,8" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Effect" Value="{StaticResource CardShadow}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="8"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource AccentSecondaryBrush}" />
                            <Setter Property="Effect" Value="{StaticResource ElevatedShadow}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
                            <Setter Property="Effect" Value="{StaticResource CardShadow}" />
                            <Setter Property="RenderTransform">
                                <Setter.Value>
                                    <ScaleTransform ScaleX="0.98" ScaleY="0.98" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Modern Card Style -->
    <Style x:Key="ModernCardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource CardBrush}" />
        <Setter Property="CornerRadius" Value="16" />
        <Setter Property="Effect" Value="{StaticResource CardShadow}" />
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <!-- Glassmorphism Card Style -->
    <Style x:Key="GlassCardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource GlassBrush}" />
        <Setter Property="CornerRadius" Value="20" />
        <Setter Property="BorderBrush" Value="{StaticResource GlassBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Effect" Value="{StaticResource CardShadow}" />
    </Style>

</ResourceDictionary>
